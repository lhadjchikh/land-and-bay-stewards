# NOTE: This docker-compose file contains development-only credentials.
# For production deployments, AWS Secrets Manager is used as described in DEPLOY_TO_ECS.md.

services:
  db:
    image: postgis/postgis:16-3.4
    volumes:
      - postgres_data:/var/lib/postgresql/data/
      - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
    environment:
      # DEVELOPMENT ENVIRONMENT ONLY - Do not use these credentials in production
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=landandbay
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      db:
        condition: service_healthy
    environment:
      # DEVELOPMENT ENVIRONMENT ONLY
      - DEBUG=True
      - SECRET_KEY=dev_secret_key_replace_in_production
      - DATABASE_URL=postgis://landandbay_app:app_password@db:5432/landandbay
      - ALLOWED_HOSTS=localhost,127.0.0.1,app
      - REACT_APP_API_URL=http://localhost:8000/api
      # Database credentials for local development
      # In production, credentials are managed through AWS Secrets Manager
      - DB_USERNAME=landandbay_admin
      - DB_PASSWORD=admin_password
      - APP_DB_USERNAME=landandbay_app
      - APP_DB_PASSWORD=app_password
    ports:
      - "8000:8000"
      - "3000:3000"
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]

volumes:
  postgres_data:
